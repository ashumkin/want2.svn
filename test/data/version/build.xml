<project name="version"
         basedir="."
         default="test" >

  <target name="test" >
    <versioninfo rcfilename="${basedir}/rcdir/sample.rc" increment="true" />
    <!-- build property has now been set to build number in .rc and can
         be accessed accordingly -->
    <echo text="build is ${build}" />
  </target>

</project>
